# Keep this file free of real credentials. Put local values in .env instead.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
# Use fixture for deterministic demos and openai only when a local API key is set.
REPAIR_PROPOSAL_PROVIDER=fixture
