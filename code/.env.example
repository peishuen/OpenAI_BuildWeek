# Qwen is the default when both values below are configured.
# Set REPAIR_PROPOSAL_PROVIDER=fixture only to explicitly use the offline fallback.
QWEN_API_KEY=
QWEN_BASE_URL=
QWEN_MODEL=qwen3.7-plus-2026-05-26
